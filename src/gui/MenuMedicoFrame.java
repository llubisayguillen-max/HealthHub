package gui;

import javax.swing.JFrame;

public class MenuMedicoFrame extends JFrame {
    public MenuMedicoFrame() {
        setTitle("Menú Administrador");
        setSize(400,300);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
